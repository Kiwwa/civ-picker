<ul>
<% @players.each do |player| %>
  <li><%= player.name %></li>
<% end %>
</ul>

<%= form_tag("/players", method: "post") do %>
  <%= label_tag(:q, "Create Player") %>
  <%= text_field_tag(:name) %>
  <%= submit_tag("Create") %>
<% end %>
